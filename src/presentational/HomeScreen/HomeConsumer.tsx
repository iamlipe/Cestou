import React from 'react';
import styled from 'styled-components/native';

export const HomeConsumer = () => {
  return (
    <StyledContainer>
      <StyledTitle>HomeConsumer</StyledTitle>
    </StyledContainer>
  );
};

const StyledContainer = styled.View``;
const StyledTitle = styled.Text``;
