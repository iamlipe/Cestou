import React from 'react';
import styled from 'styled-components/native';

export const HomeProducer = () => {
  return (
    <StyledContainer>
      <StyledTitle>HomeProducer</StyledTitle>
    </StyledContainer>
  );
};

const StyledContainer = styled.View``;
const StyledTitle = styled.Text``;
