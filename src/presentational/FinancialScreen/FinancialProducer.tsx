import React from 'react';
import styled from 'styled-components/native';

export const FinancialProducer = () => {
  return (
    <StyledContainer>
      <StyledText>FinancialProducer</StyledText>
    </StyledContainer>
  );
};

const StyledContainer = styled.View``;
const StyledText = styled.Text``;
